# знак $- означает для парсера - однострочное значение параметра
$action=select
$scheme=buses1
$prompt=0

# знак @- означает многострочное значение параметра
# в первой строке - параметр
# в последующих - значение этого параметра, пока не встретится в начале строки
# символ $ или @, означающее новый параметр.

@sql
select distinct bus_number from buses inner join trips on buses.cod_bus=trips.cod_bus where
trips.week_day>=6 order by bus_number
# если запрос нужно отсортировать:
# если не нужно -> $order=0, либо вообще убрать этот параметр
$order=1
